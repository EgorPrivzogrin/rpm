if __name__=="__main__":
  from math import sqrt
a =float(input("коэффицент а"))
b =float(input("коэффицент b" ))
c =float(input("коэффицент c"))
D = b ** 2 - (4 * a * c)
if D < 0:
    print('Корней нет')
elif D > 0:
    x1, x2 = (-b + sqrt(D)) / (2 * a), (-b - sqrt(D)) / (2 * a)
    print(min(x1, x2))
    print(max(x1, x2))
elif D == 0:
    print(-b / (2 * a))
